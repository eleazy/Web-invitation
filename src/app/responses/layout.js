import React from "react";

export const metadata = {
  title: "Convite Respostas",
  description: "Website convite",
};

export default function ResponsesLayout({ children }) {
  return <main>{children}</main>;
}
